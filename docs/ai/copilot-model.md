# AI Copilot Model (Phase 3)

Contextual side panel via ⌘K. Scope from route + tenant.

| Portal | Example |
|--------|---------|
| Platform | Underperforming brands |
| Brand | Centers needing intervention |
| Center | Enrollment decline reasons |

Uses Vercel AI SDK + read-only Supabase queries (RLS client). Cache in `ai_insights`.
